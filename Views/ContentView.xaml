<UserControl x:Class="PrismSample.Views.ContentView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:PrismSample.Views"
             mc:Ignorable="d"
             xmlns:dryIoc="http://prismlibrary.com/"
             dryIoc:ViewModelLocator.AutoWireViewModel="True"
             d:DesignHeight="300" d:DesignWidth="300">
    <Grid>
        <TextBlock Text="{Binding Title}"></TextBlock>
        <Button Command="{Binding OpenCommand}">按钮</Button>
    </Grid>
</UserControl>
